# Google Cloud Configuration
# Set your Google Cloud project ID
GOOGLE_CLOUD_PROJECT=your-project-id

# Set your preferred region for Dataflow operations
GOOGLE_CLOUD_REGION=us-central1

# Google Cloud credentials (optional - uses default gcloud auth if not set)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# ADK Configuration
# Your Google AI API key for Gemini model
# GOOGLE_AI_API_KEY=your-google-ai-api-key

# Optional: Set logging level
LOG_LEVEL=INFO

# Optional: Set default timeout for gcloud commands (in seconds)
GCLOUD_TIMEOUT=300

# Optional: Set default timeout for agent commands (in seconds)
AGENT_TIMEOUT=600
